<table border="1">
  <tr>
    <th>Имя</th>
    <th>Телефон</th>
    <th>Время и дата</th>
    <th>Парикмахер</th>
    <th>Цвет</th>
  </tr>

<% @result.each do |row| %>

  <tr>
    <td><%= row['Name'] %></td>
    <td><%= row['Phone'] %></td>
    <td><%= row['DateStamp'] %></td>
    <td><%= row['Barber'] %></td>
    <td><%= row['Color'] %></td>
  </tr>

<% end %>

</table>